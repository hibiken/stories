<div class="like-button-wrapper">
  <% if user_signed_in? %>
    <% if current_user.liked?(post) %>
      <%= button_to post_like_path(post, current_user.likes.find_by(likeable: post)), method: :delete, remote: true, class: 'unlike-button' do %>
        <span class="icon-heart2"></span><span class="hide-text">Unlike</span>
      <% end %>
    <% else %>
      <%= button_to post_likes_path(post), remote: true, class: 'like-button' do %>
        <span class="icon-heart-o"></span><span class="hide-text">Like</span>
      <% end %>
    <% end %>
  <% else %>
    <%= button_to '', method: :get, class: 'like-button', data: { behavior: 'trigger-overlay' } do %>
      <span class="icon-heart-o"></span><span class="hide-text">Like</span>
    <% end %>
  <% end %>
</div>
<% if post.reload.likes.size > 0 %>
  <% if user_signed_in? %>
    <span class="like-count"><%= react_component('OverlayTriggerButton', { text: post.likes.size, post_id: post.id, title: post.title }) %></span>
  <% else %>
    <span class="like-count"><%= post.likes.size %></span>
  <% end %>
<% end %>

