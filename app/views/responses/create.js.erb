$('[data-behavior="responses-list"]').prepend('<%=j render partial: 'responses/response', locals: { response: @response } %>');
$('[data-behavior="editor-cancel"]').trigger('click');
$('[data-behavior="response-count"]').text('<%= @post.responses.count.to_s %>');

ReactRailsUJS.mountComponents();
